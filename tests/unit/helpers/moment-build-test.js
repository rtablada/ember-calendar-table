import { momentBuild } from '../../../helpers/moment-build';
import { module, test } from 'qunit';

module('Unit | Helper | moment build');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentBuild(42);
  assert.ok(result);
});
